#!/usr/bin/env ruby

require 'renumber'

include Renumber

puts 'Usage: renumber directory_name [prefix] [suffix]' and return unless ARGV[0]

begin
  change_files ARGV[0], ARGV[1], ARGV[2]
rescue ArgumentError => e
  puts e.message
end